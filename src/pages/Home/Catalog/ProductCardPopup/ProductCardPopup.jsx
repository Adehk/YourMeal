import styles from "./ProductCardPopup.module.sass";

const ProductCardPopup = () => {
  return <div className={styles.productCardPopup}>
    <h2 className={styles.productCardPopup__title}></h2>
  </div>
};

export default ProductCardPopup;